# WIP
# Intended to asynchronously read/write a config file while the service is going to store settings.
import sys
import os
import configparser
from pathlib import Path
import datetime
# import watchdog # Watchdog is used to track when a file changes in some way, and act on it as a callback.

def generate_settings_ini():
    pass